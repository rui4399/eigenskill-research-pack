# NotebookLM Enterprise Sources

These files are ready to import into NotebookLM. If Enterprise API OAuth is available, use `notebooklm_enterprise_user_contents.json` and pass its `user_contents` to `notebooklm_enterprise_add_sources`.

Source count: 12
