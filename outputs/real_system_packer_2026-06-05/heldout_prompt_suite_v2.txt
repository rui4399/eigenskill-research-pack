# Third prompt split for layer20 V rowguard transfer validation.
# Keep task family comparable, but avoid reusing the original/v1 wording.

Explain calibration-set sensitivity in mixed-bit LLM quantization using exactly two sentences.
Produce minified JSON with keys policy, evidence, and caveat for a precision-routing experiment.
How many bytes store 4096 four-bit signed weights when packed densely? Give only the number and unit.
Write a C++ declaration for a function that multiplies selected quantized output rows by a dense activation vector.
Give two distinct ways a row-level precision search can overfit a tiny generation prompt suite.
In one sentence, explain why INT8 protection of an attention value projection may still reduce exact-match fidelity.
Draft a four-step ablation plan for testing whether a precision guard transfers across prompt splits.
Compare activation reconstruction error and generated-text prefix match in one compact paragraph.
Name two deployment metrics for an edge LLM runtime besides model size and perplexity.
Write a cautious README sentence explaining that a packed kernel benchmark is not an end-to-end acceleration claim.
Describe how hidden-state drift could be used as a cheap proxy for selecting sensitive rows.
State one reason an early transformer layer may need a different quantization policy than middle layers.
